# Import the logging module for logging operations
import logging

# Create a logger instance for 'uvicorn'
# Set the logging level to INFO to capture informational messages
logger = logging.getLogger("uvicorn")
logger.setLevel(logging.INFO)
